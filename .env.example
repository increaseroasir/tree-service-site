# Server-only. In AI Studio, add these under Cloud → Secrets (never VITE_-prefixed;
# they must not reach the browser). Locally, copy to .env.
# Register a "Service Type" custom field in the client's GHL location and paste its id.
GHL_TRACKING_ID=
GHL_LOCATION_ID=
GHL_PROJECT_ID=
GHL_SERVICE_TYPE_FIELD_ID=
